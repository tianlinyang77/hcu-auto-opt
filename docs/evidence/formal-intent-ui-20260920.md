# Formal 意图入口 UI 功能检查

日期：2026-09-20；关联 #167 / #168。

## 范围

生产前端构建 + FastAPI + 既有 FormalStartCoordinator 的本地浏览器联验。
使用临时内存仓库、测试签名器和固定测试时钟；页面顶部明确标注测试夹具。
不使用生产凭据，不操作现有数据库或 HCU，不代表生产授权或性能验收。

## 已观察结果

1. 未输入独立凭据时，读取按钮禁用。
2. 读取部署绑定请求后显示 Preview、请求编号、授权 Hash 和服务实例；内容不可编辑。
3. 未勾选“仅提交意图，不启动 HCU”时，提交按钮禁用。
4. 明确确认并提交后，显示“授权核对完成，尚未创建轮次”。
5. 回执 Intent 为 `8adab440-7fe6-5fee-aa2c-43a2074e33f0`，同时提示未派发 Worker。
6. 提交后凭据输入清空，提交按钮禁用。截图检查显示中文字体及 Hash 可读。

## 自动化检查

- API 定向测试：23 passed（含受保护准备接口、有效期与请求范围绑定）。
- Web lint、单元测试、生产构建、Sites 打包测试通过。
- 前端测试覆盖原请求重试、禁止字段升级、回执身份替换和错误信息脱敏。
- 上一提交 `3190c65` 的 CI 六项通过，并在真实 PostgreSQL 17 中验证 Intent 并发与重载；
  本次扩展的准备接口 PostgreSQL 测试仍需以新提交 CI 结果为准。
- 另一次本机 Windows 全量探索运行报告 1718 passed、46 skipped、18 failed；失败涉及源码/制品
  测试，已见 `WinError 1314`（创建符号链接权限不足），其余尚未逐项归因。
  该运行期间工作树继续开发，不作为本提交的固定版本验收；不宣称本机全量通过，也未修改系统权限。

## 尚未完成

生产凭据签发和部署装配、生产数据库浏览器联验、正式 Round 事务派发和 HCU 执行。
本报告不批准生产启用，不替代 ADR 上下游评审。
